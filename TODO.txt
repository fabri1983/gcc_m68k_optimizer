
Examples of ABI calls:
----------------------
Case 1:
	move.l %d1,-(%sp)
	eor.w %d1,%d2
	move.l %d2,-(%sp)
	move.l %d0,-(%sp)
	jsr (%a0)
    ... code with arithmetic over SP ...

Case 2:
	move.l %d0,-(%sp)
	move.l %a0,-(%sp)
	jsr SYS_getFarDataSafe
	... lot of instructions but none jsr ...
    ... code with arithmetic over SP ...

Case 3:
	pea 6400.w
	move.l %a0,-(%sp)
	lea SYS_getFarDataSafe,%a5
	move.l %d1,52(%sp)
	jsr (%a5)
	... lot of instructions but none jsr ...
    ... code with arithmetic over SP ...

Case 4:
    move.l %d0,-(%sp)
	jsr (%a5)
	... lot of instructions but none jsr ...
    ... code with arithmetic over SP ...

Case 5:
	move.l %d0,-(%sp)
	jsr MEM_free
	... code with arithmetic over SP ...

jsr/bsr/bra/jra/jmp
Arithmetic over SP: instructions involving disp(%sp) or (disp,%sp) or (%sp), but not -(%sp) neither (%sp)+
Where disp is an integer displacement: value or -value
PEA val_or_symbol must be treated as: move.l #val_or_symbol,-(%sp)
LINK aN,#val must be treated as: move.w  aN,-(sp)
                                 move.w  sp,aN
                                 add.w   #val,sp
UNLK aN must be treated as: move.l  aN,sp
                            move.l  (sp)+,aN



SGDK SAMPLES:
Tested on Blastem nightly build
	Crash or Fail:
		benchmark -> Crash after Horizontal Scrolling (alternative/alternate). I think it was test 7th.
		flash-save -> Gives HW Error text on display
		game/sonic -> Crash when pressing start
		snd/xgm-player -> Crash when loading rom
	Other:
		megawifi -> Needs Emulator with Megawifi support?
		sys/console -> Needs MODULE_CONSOLE set to 1



TODO
====
Review USE_REPLACE_LOAD_SUBROUTINE_INTO_AN_BY_CALLING_SUBROUTINE_DIRECTLY when is True.

Review add_regs_into_push_pop_if_not_scratch_or_in_interrupt().

Reg expressions catching a symbol or mem address along with its size: [0-9a-zA-Z_\.]+)(\.[bwl])?
Make sure the size is not part of the symbolName or the mem address.
    # Remove the size if it ended up being part of the symbol
    if symbolName.endswith(('.l','.w','.b')):
        symbolName = symbolName[:-2]  # remove last 2 chars
    # Remove the size if it ended up being part of the mem address
    if mem_addr.endswith(('.l','.w','.b')):
        mem_addr = mem_addr[:-2]  # remove last 2 chars

When generating an int with int(string) or parseConstantSigned(string) or evaluate_instr_math_expression() 
and then comparing against an hex value 0xAACCEE00:
    Use & 0xFFFFFFFF over the int value to remove the sign that python sets at 64th bit

Tener en cuenta siempre que si se deja de usar un registro hay q usar:
	if_reg_not_used_anymore_then_remove_from_push_pop(<reg>, i_line, lines, modified_lines, <ingore_N_previous_lines>)

Ver los TODO en el optimizer script.

Test the new division optimization.



Single instruction
==================
Clear upper word of dN.
We must know for sure dN holds val such that: (val & 0xFFFFFFFF) <= SGDK_LOW_ROM_END or (val & 0xFFFFFFFF) >= SGDK_HIGH_RAM_START
and.l   #0xFFFF,dN   ->   ext.l   dN             ; Saves 12 cycles

Clearing memory using existent dN already holding 0 value.
Must find a dN holding 0 in bits s.
clr.s   -(aN)        ->   move.s  dN,-(aN)       ; Saves [6,10] cycles

Replace usage of immediate by register.
Must find a dM holding val in bits s.
btst.s  #val,dN      ->   btst.s  dM,dN          ; Saves 4 cycles

Multiplication by a negative constant: see the comments in the optimizer script and implement accordingly.



Multiline instructions
======================
# Use of unnecessary additional data register.
move.b  (aN),dM    ->    or.b    (aN),dN         ; Saves 4 cycles. Leaves dM free
and.w   #255,dM          and.w   #255,dN
or.b    dM,dN
Leaves dM free. Ensure dM is not used before is cleared or overwritten.

Next optimization only valid for 3 or more disp(sp).
Adding same immediate value into subsequent memory by indirect addressing.
    addq.w #1,44(sp)
    might be 1 or 2 instructions here of the form: addq/add/adda.[wl] #val,aN  (aN != sp)
    addq.w #1,46(sp)
    might be 1 or 2 instructions here of the form: addq/add/adda.[wl] #val,aN  (aN != sp)
    addq.w #1,48(sp)
; Saves 4 cycles
    lea 44(sp),aM
    addq.w #1,(a0)+
    instructions gathered in-between (if any)
    addq.w #1,(a0)+
    instructions gathered in-between (if any)
    addq.w #1,(a0)
Needs a free address register aM.


Assembler useful functions
==========================
# Sign: sets d1=0 if d0=0, d1=1 if d0>0, or d1=-1 if d0<0
add.s   d0,d0
subx.s  d1,d1
negx.s  d0
addx.s  d1,d1
 
# Check if A=B, (d0=A, d1=B, range 0000-7FFF, but true in all cases if B=$ffff)
eor.w   d1,d0
bgt     not_equal
# Note that d0 is overwritten.

# If the carry bit is set, then dN=0xFF, otherwise dN=0x00
scs[.b] dN     ->   subx.l dN,dN         ; Saves [4,6] cycles
ext.w   dN
ext.l   dN

# Logical Test. d0=0 if d0 == 0 else, d0=-1 if d0 != 0
# By prepending with 'move.l A,d0; sub.l B,d0' you can construct tests for A == B and A != B
neg.s   d0
subx.s  d0,d0

# Logical Test. d0=0 if d0 == 0 else, d0=1 if d0 != 0
# By prepending with 'move.l A,d0; sub.l B,d0' you can construct tests for A == B and A != B
neg.s   d0
subx.s  d0,d0
neg.s   d0

# Logical Test. d0=-1 if d0 == 0 else, d0=0 if d0 != 0
# By prepending with 'move.l A,d0; sub.l B,d0' you can construct tests for A == B and A != B
neg.s   d0
subx.s  d0,d0
not.s   d0

# Logical Test. d0=1 if d0 == 0 else, d0=0 if d0 != 0
# By prepending with 'move.l A,d0; sub.l B,d0' you can construct tests for A == B and A != B
neg.s   d0
subx.s  d0,d0
addq.s  1,d0

# Absolute value: abs(x). Needs a free reg d1.
move.s  d0,d1
add.s   d1,d1
subx.s  d1,d1
eor.s   d1,d0
sub.s   d1,d0
# Result: abs(x) in d0

# Max of 2 unsigned nums. Needs a free reg d2.
# Where d0=X, d1=Y
sub.s   d1,d0
subx.s  d2,d2
or.s    d2,d0
addx.s  d1,d0
# Result: d0 = max(X,Y)

# Min of 2 unsigned nums. Needs a free reg d2.
# Where d0=X, d1=Y
sub.s   d1,d0
subx.s  d2,d2
and.s   d2,d0
add.s   d1,d0
# Result: d0 = min(X,Y)

# Simultaneous Max and Min of 2 unsigned nums. Needs a free reg d2.
# Where d0=X, d1=Y
sub.s   d1,d0
subx.s  d2,d2
and.s   d0,d2
eor.s   d2,d0
add.s   d1,d0
add.s   d2,d1
# Results: d0 = max(X,Y), d1 = min(X,Y)
